name: LeeHollow RustDesk Test Build

on:
  workflow_dispatch:

permissions:
  contents: write
  actions: read

jobs:
  build:
    uses: ./.github/workflows/flutter-build.yml
    with:
      upload-artifact: true
      upload-tag: leehollow-test
      windows-only: true
    secrets: inherit
